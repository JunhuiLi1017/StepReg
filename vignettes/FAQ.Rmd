---
title: "FAQ for StepReg"
output: rmarkdown::html_vignette
vignette: >
  %\VignetteIndexEntry{FAQ for StepReg}
  %\VignetteEngine{knitr::rmarkdown}
  %\VignetteEncoding{UTF-8}
---

# Frequently Asked Questions

**Q: How do I install StepReg efficiently?**  
A: You can install StepReg using the following command:

```r
pak::pkg_install("StepReg")
```

or

```r
install.packages("StepReg")
```

or

```r
devtools::install_github("JunhuiLi1017/StepReg")
```
we recommend using `pak::pkg_install("StepReg")` to install StepReg to avoid the dependency issues.

**Q: I encounter a missing system libraries error when installing StepReg. What should I do?**  
A: Some dependencies of StepReg require additional system libraries to be installed on your computer. If you see an error message about missing system packages, you may need to manually install them using your system package manager.

For common dependencies, you may need to install:

- cairo-devel (required for gdtools)
- ImageMagick-c++-devel and ImageMagick-devel (required for magick)
- libwebp-devel (required for ragg)
- X11 library (required for graphics on macOS): If the X11 library is missing (often required for graphics on macOS), please install XQuartz first.

installation examples:

Ubuntu/Debian:
```sh
sudo apt-get install libcairo2-dev libwebp-dev libmagick++-dev
```

Red Hat/CentOS:
```sh
sudo yum install cairo-devel libwebp-devel ImageMagick-c++-devel ImageMagick-devel
```

macOS:
You can use Homebrew to install the dependencies (cairo, imagemagick, and webp):
```sh
brew install cairo imagemagick webp
```
